# ============================================================
# Docker Compose Configuration for Daishan Services
# Local Development
# ============================================================
# Usage:
#   docker compose up -d          # Start both services
#   docker compose logs -f        # Follow logs
#   docker compose down           # Stop and remove containers
# ============================================================

services:
  # ============================================================
  # rag_stream 服务
  # ============================================================
  rag_stream:
    image: ${RAG_STREAM_IMAGE:-daishan/rag_stream:local}
    build:
      context: ../..              # Project root for workspace dependencies
      dockerfile: src/rag_stream/Dockerfile
      target: runtime
      args:
        APP_ENV: ${APP_ENV:-development}
    
    container_name: rag_stream
    
    ports:
      - "11027:11027"
    
    env_file:
      - ../../src/Digital_human_command_interface/.env
    
    environment:
      - LOG_CONSOLE_OUTPUT=true   # Enable stdout logging in container
    
    volumes:
      # Mount config.yaml for runtime configuration changes (read-only)
      - ../../src/rag_stream/config.yaml:/app/src/rag_stream/config.yaml:ro
    
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:11027/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    
    restart: unless-stopped
    
    # Graceful shutdown configuration - matches Uvicorn 30s timeout
    stop_signal: SIGTERM
    stop_grace_period: 30s
    
    # Resource hints for development
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 2G
        reservations:
          cpus: '0.5'
          memory: 512M
    
    # Logging configuration
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  # ============================================================
  # Digital_human_command_interface 服务（意图识别）
  # ============================================================
  digital_human:
    build:
      context: ../..              # Project root for workspace dependencies
      dockerfile: src/Digital_human_command_interface/Dockerfile
    
    container_name: digital_human
    
    ports:
      - "11029:11029"
    
    env_file:
      - path: ../../.env          # Load environment variables from project root when present
        required: false
    
    environment:
      - RAG_STREAM_BASE_URL=http://rag_stream:11027  # 服务间通信使用容器名
      - LOG_CONSOLE_OUTPUT=true
      - RAGFLOW_API_KEY=${RAGFLOW_API_KEY}
    
    volumes:
      # Mount config.yaml for runtime configuration changes (read-only)
      - ../../src/Digital_human_command_interface/config.yaml:/app/src/Digital_human_command_interface/config.yaml:ro
    
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:11029/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    
    restart: unless-stopped
    
    # Graceful shutdown configuration - matches Uvicorn 30s timeout
    stop_signal: SIGTERM
    stop_grace_period: 30s
    
    # 依赖 rag_stream 服务
    depends_on:
      rag_stream:
        condition: service_healthy
    
    # Resource hints
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 1G
        reservations:
          cpus: '0.25'
          memory: 256M
    
    # Logging configuration
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

# ============================================================
# 网络配置（默认 bridge）
# ============================================================
networks:
  default:
    name: daishan_network
    driver: bridge
