# 1. 创建环境
conda create -n rag_stream python=3.9 -y

# 2. 激活环境
conda activate rag_stream

# 3. 安装依赖
pip install -r requirements.txt

# 4. 启动服务
python start.py

# 5. 测试接口
python test_client.py

# 6. 快速测试
python quick_test.py

nohup uvicorn main:app --host 0.0.0.0 --port 11027 > ./logfile.log 2>&1 &
