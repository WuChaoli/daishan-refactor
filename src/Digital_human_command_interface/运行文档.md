运行main.py

```
python main.py
```

```
nohup python main.py > ./main.log 2>&1 &
```

关闭进程

```
# 查看后台进程（找到 python main.py 的 PID）
ps aux | grep "python main.py"

# 停止进程（替换 PID 为实际进程号）
kill -9 PID
```

退出conda环境

```
conda deactivate
```

杀死进程
pkill -f "python.*main.py" || true