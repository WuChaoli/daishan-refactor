# <Skill Title>

```yaml
---
name: <skill-name>
description: <一句话能力概述>。用于<任务范围/文件范围>；当用户提到"<关键词1>"、"<关键词2>"或需要<场景>时使用。
---
```

## 核心目标

用 1-2 句话说明该技能要稳定解决的重复问题。

## 必须遵守

1. 规则 1（硬约束）
2. 规则 2（硬约束）
3. 规则 3（硬约束）

## 执行流程

### 1) 输入确认

- 输入项 A
- 输入项 B

### 2) 实施步骤

- 步骤 A
- 步骤 B
- 步骤 C

### 2.1) 自动化脚本（可选）

- 脚本入口：`scripts/...`
- 调用命令：`<runtime> scripts/<file> <args>`
- I/O 约定：输入来源与输出路径
- 最小验证：至少给 1 条可执行验证命令

### 3) 结果交付

- 产出文件列表
- 关键决策说明

## 校验清单

- [ ] frontmatter 仅包含 `name` 与 `description`
- [ ] `description` 包含触发条件与关键词
- [ ] 正文无不可用工具或平台专属机制
- [ ] 输出可被命令或清单验证
- [ ] 若包含 `scripts/`，调用命令和参数说明完整
- [ ] 若包含 `scripts/`，最小验证命令可执行

## 失败回退

- 信息不足时如何提问
- 权限受限时如何暂停并请求确认

## 参考文件（可选）

- `assets/...`
- `references/...`
- `scripts/...`
