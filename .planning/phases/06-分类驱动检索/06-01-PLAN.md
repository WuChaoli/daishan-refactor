---
phase: 06-分类驱动检索
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/rag_stream/src/config/settings.py
  - src/rag_stream/config.yaml
autonomous: true
requirements:
  - CLS-03
user_setup: []

must_haves:
  truths:
    - "系统在置信度 >= 0.7 时，仅检索对应类型的向量库"
    - "系统在置信度 < 0.7 或降级时，执行全量检索保持向后兼容"
    - "分类调用失败时，系统记录降级标记并走全量检索流程"
    - "不同意图类型的查询不会跨库混淆检索结果"
  artifacts:
    - path: "src/rag_stream/src/config/settings.py"
      provides: "IntentClassificationConfig with confidence_threshold field"
      contains: "confidence_threshold: float"
    - path: "src/rag_stream/src/services/intent_service/intent_service.py"
      provides: "Classification-driven retrieval logic"
      contains: "classify() call in _load_process_settings"
    - path: "src/rag_stream/config.yaml"
      provides: "Updated intent_classification config block"
      contains: "confidence_threshold: 0.7"
  key_links:
    - from: "intent_service._load_process_settings"
      to: "IntentClassifier.classify"
      via: "Classifier call before retrieval"
      pattern: "self._intent_classifier\.classify\(.*\)"
    - from: "base_intent_service._query_table_results"
      to: "filtered database_mapping"
      via: "Type-based filtering"
      pattern: "table_names = list\(filtered_mapping\.keys\(\)\)"
---

<objective>
实现两阶段识别机制：粗分类后仅在对应类型的向量库中检索，解决语义混淆问题

Purpose: 将 Phase 5 实现的 IntentClassifier 与现有向量检索流程连接，实现分类驱动的精准检索
Output: 置信度阈值配置 + 分类调用逻辑 + 库级过滤检索
</objective>

<execution_context>
@/home/wuchaoli/.claude/get-shit-done/workflows/execute-plan.md
@/home/wuchaoli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-意图分类基础/05-01-SUMMARY.md
@.planning/phases/05-意图分类基础/05-03-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Phase 5 -->

From src/rag_stream/src/utils/intent_classifier.py:
```python
@dataclass
class ClassificationResult:
    type_id: int  # 1/2/3
    confidence: float  # 0.0-1.0
    raw_response: str  # 原始响应
    degraded: bool  # 是否降级

class IntentClassifier:
    def __init__(self, config: IntentClassificationConfig)
    def classify(self, query: str) -> ClassificationResult
```

From src/rag_stream/src/services/intent_service/intent_service.py:
```python
class IntentService(BaseIntentService):
    def __init__(self, ...):
        self._intent_classifier: IntentClassifier | None = None  # 已初始化
```

From src/rag_stream/src/config/settings.py:
```python
class IntentClassificationConfig:
    enabled: bool
    api_key: str
    base_url: str
    model: str
    timeout: int
    temperature: float
    confidence_threshold: float  # 需在 Task 1 中添加
```
</interfaces>

From src/rag_stream/src/services/intent_service/base_intent_service.py:
```python
class IntentRecognizerSettings:
    database_mapping: Mapping[str, Any]  # {"岱山-指令集": 1, "岱山-数据库问题": 2, ...}
    similarity_threshold: float
    top_k: int
    default_type: int
```
</interfaces>

# Existing integration points

## IntentService._load_process_settings (line 107-118)
```python
def _load_process_settings(self) -> IntentRecognizerSettings:
    recognizer_settings = self._load_intent_recognizer_settings()
    self._use_daishan_priority = self._should_use_daishan_priority(
        recognizer_settings
    )

    # 预留：可选调用分类服务（Phase 6 使用）
    # if self._intent_classifier:
    #     classification_result = self._intent_classifier.classify(text_input)
    #     ...

    return recognizer_settings
```

## BaseIntentService._query_table_results (line 341-372)
```python
async def _query_table_results(
    self,
    query: str,
    recognizer_settings: IntentRecognizerSettings,
) -> Dict[str, List[Any]]:
    """并发查询映射中所有表"""
    if self.ragflow_client is None:
        raise ValueError("RagflowClient not initialized")

    if not hasattr(self.ragflow_client, "query_single_database"):
        raise ValueError("RagflowClient missing query_single_database method")

    table_names = list(recognizer_settings.database_mapping.keys())  # 当前全量查询
    tasks = [
        self.ragflow_client.query_single_database(query, table_name)
        for table_name in table_names
    ]
    task_results = await asyncio.gather(*tasks, return_exceptions=True)

    # ... 错误处理 ...
```

# Database mapping structure (from config.yaml)
```yaml
database_mapping:
  岱山-指令集: 1
  岱山-数据库问题: 2
  岱山-指令集-固定问题: 3
```

# Type ID to database name mapping
- type_id=1 → "岱山-指令集"
- type_id=2 → "岱山-数据库问题"
- type_id=3 → "岱山-指令集-固定问题"
</context>

<tasks>

<task type="auto">
  <name>Task 1: 添加置信度阈值配置字段</name>
  <files>src/rag_stream/src/config/settings.py, src/rag_stream/config.yaml</files>
  <action>
    1. 在 IntentClassificationConfig 类中添加 confidence_threshold 字段（当前在 plan 05-01 中已存在，但需验证）
    2. 确认字段验证规则：必须在 0-1 之间（已存在）
    3. 更新 config.yaml 中的 confidence_threshold 默认值为 0.7（当前是 0.5，需改为 0.7）

    注意：如果 confidence_threshold 字段已存在，仅需验证并更新默认值；如果不存在，需添加字段和验证规则。
  </action>
  <verify>
    <automated>grep -n "confidence_threshold" src/rag_stream/src/config/settings.py src/rag_stream/config.yaml</automated>
  </verify>
  <done>IntentClassificationConfig 包含 confidence_threshold 字段，默认值为 0.7，验证规则在 [0.0, 1.0] 范围内</done>
</task>

<task type="auto">
  <name>Task 2: 实现分类驱动检索逻辑</name>
  <files>src/rag_stream/src/services/intent_service/intent_service.py, src/rag_stream/src/services/intent_service/base_intent_service.py</files>
  <action>
    在 IntentService 中实现分类驱动的检索逻辑：

    1. **修改 _load_process_settings 方法**：
       - 添加 text_input 参数（从调用方传入）
       - 检查 self._intent_classifier 是否存在且配置启用
       - 如果启用，调用 self._intent_classifier.classify(text_input)
       - 获取 classification_result.type_id 和 classification_result.confidence
       - 获取 settings.intent_classification.confidence_threshold 阈值
       - 判断是否应用分类结果：
         * 如果 classification_result.degraded=True 或 confidence < threshold：返回未过滤的 recognizer_settings（全量检索）
         * 否则：过滤 recognizer_settings.database_mapping，仅保留 value == type_id 的键值对

    2. **标记降级行为**：
       - 当 degraded=True 时，记录 marker "classifier.degraded_fallback"，包含 {"query": text_input, "reason": "degraded"}
       - 当 confidence < threshold 时，记录 marker "classifier.low_confidence_fallback"，包含 {"query": text_input, "confidence": confidence, "threshold": threshold}

    3. **返回值**：
       - 修改方法签名，返回 (IntentRecognizerSettings, str)，其中 str 是过滤后的 mapping JSON（用于调试）或 None（未过滤）

    4. **修改 BaseIntentService._query_table_results**：
       - 不需要修改（接收已过滤的 recognizer_settings）

    5. **调用方适配**：
       - 找到 _load_process_settings 的调用点（可能在 recognize_intent 或 query_process 相关方法）
       - 传入 text_input 参数
       - 接收返回的 (recognizer_settings, filtered_mapping) 元组

    关键实现细节：
    - 使用字典推导式过滤 database_mapping：{k: v for k, v in mapping.items() if v == type_id}
    - 保持向后兼容：未启用分类或降级时，返回完整的 recognizer_settings
  </action>
  <verify>
    <automated>grep -n "classify(" src/rag_stream/src/services/intent_service/intent_service.py</automated>
  </verify>
  <done>
    - IntentService._load_process_settings 接收 text_input 参数
    - 调用 IntentClassifier.classify 进行分类
    - 置信度 >= 0.7 时过滤 database_mapping，仅保留对应类型的库
    - 置信度 < 0.7 或降级时返回完整 database_mapping
    - 降级和低置信度场景记录 marker 日志
  </done>
</task>

</tasks>

<verification>
1. 配置验证：检查 confidence_threshold 字段存在且默认值为 0.7
2. 分类调用验证：grep 搜索 classify() 调用点，确认在 _load_process_settings 中
3. 过滤逻辑验证：检查 database_mapping 过滤逻辑，确认使用字典推导式
4. Marker 验证：搜索 "classifier.degraded_fallback" 和 "classifier.low_confidence_fallback" 标记
5. 向后兼容验证：未启用分类时，database_mapping 保持完整
</verification>

<success_criteria>
1. 配置文件包含 confidence_threshold 字段，默认值为 0.7
2. IntentService 在检索前调用分类器获取 type_id 和 confidence
3. 置信度 >= 0.7 时，仅检索对应类型的向量库（通过过滤 database_mapping 实现）
4. 置信度 < 0.7 或降级时，执行全量检索（向后兼容）
5. 降级和低置信度场景记录 marker 日志，便于监控
6. 分类失败不影响主流程，系统保持可用性
</success_criteria>

<output>
After completion, create `.planning/phases/06-分类驱动检索/06-01-SUMMARY.md`
</output>
