---
phase: 06-分类驱动检索
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/rag_stream/src/config/settings.py
  - src/rag_stream/config.yaml
  - src/rag_stream/src/services/intent_service/intent_service.py
  - src/rag_stream/tests/test_intent_service_classification.py
autonomous: true
requirements:
  - CLS-03
user_setup: []

must_haves:
  truths:
    - "系统在置信度 >= 0.7 时，仅检索对应类型的向量库"
    - "系统在置信度 < 0.7 或降级时，执行全量检索保持向后兼容"
    - "分类调用失败时，系统记录降级标记并走全量检索流程"
    - "不同意图类型的查询不会跨库混淆检索结果"
  artifacts:
    - path: "src/rag_stream/src/config/settings.py"
      provides: "IntentClassificationConfig with confidence_threshold field"
      contains: "confidence_threshold: float"
    - path: "src/rag_stream/config.yaml"
      provides: "Updated intent_classification config block"
      contains: "confidence_threshold: 0.7"
    - path: "src/rag_stream/src/services/intent_service/intent_service.py"
      provides: "Classification-driven retrieval logic"
      contains: "classify() call in _load_process_settings"
    - path: "src/rag_stream/tests/test_intent_service_classification.py"
      provides: "Test coverage for classification-driven retrieval"
      contains: "test_classification_driven_retrieval"
  key_links:
    - from: "intent_service._load_process_settings"
      to: "IntentClassifier.classify"
      via: "Classifier call before retrieval"
      pattern: "self._intent_classifier\.classify\(.*\)"
    - from: "intent_service._load_process_settings"
      to: "filtered database_mapping"
      via: "Type-based filtering"
      pattern: "database_mapping = \{.*\}"
    - from: "base_intent_service._query_table_results"
      to: "filtered database_mapping"
      via: "Using filtered keys"
      pattern: "table_names = list\(filtered_mapping\.keys\(\)\)"
---

<objective>
实现两阶段识别机制：粗分类后仅在对应类型的向量库中检索，解决语义混淆问题

Purpose: 将 Phase 5 实现的 IntentClassifier 与现有向量检索流程连接，实现分类驱动的精准检索
Output: 置信度阈值配置 + 分类调用逻辑 + 库级过滤检索 + 测试覆盖
</objective>

<execution_context>
@/home/wuchaoli/.claude/get-shit-done/workflows/execute-plan.md
@/home/wuchaoli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-意图分类基础/05-01-SUMMARY.md
@.planning/phases/05-意图分类基础/05-03-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Phase 5 -->

From src/rag_stream/src/utils/intent_classifier.py:
```python
@dataclass
class ClassificationResult:
    type_id: int  # 1/2/3
    confidence: float  # 0.0-1.0
    raw_response: str  # 原始响应
    degraded: bool  # 是否降级

class IntentClassifier:
    def __init__(self, config: IntentClassificationConfig)
    def classify(self, query: str) -> ClassificationResult
```

From src/rag_stream/src/services/intent_service/intent_service.py:
```python
class IntentService(BaseIntentService):
    def __init__(self, ...):
        self._intent_classifier: IntentClassifier | None = None  # 已初始化
```

From src/rag_stream/src/config/settings.py:
```python
class IntentClassificationConfig:
    enabled: bool
    api_key: str
    base_url: str
    model: str
    timeout: int
    temperature: float
    confidence_threshold: float  # 需在 Task 1 中更新为 0.7
```

From src/rag_stream/src/services/intent_service/base_intent_service.py:
```python
class IntentRecognizerSettings:
    database_mapping: Mapping[str, Any]  # {"岱山-指令集": 1, "岱山-数据库问题": 2, ...}
    similarity_threshold: float
    top_k: int
    default_type: int
```
</interfaces>

# Existing integration points

## IntentService._load_process_settings (line 107-118)
```python
def _load_process_settings(self) -> IntentRecognizerSettings:
    recognizer_settings = self._load_intent_recognizer_settings()
    self._use_daishan_priority = self._should_use_daishan_priority(
        recognizer_settings
    )

    # 预留：可选调用分类服务（Phase 6 使用）
    # if self._intent_classifier:
    #     classification_result = self._intent_classifier.classify(text_input)
    #     ...

    return recognizer_settings
```

## BaseIntentService._query_table_results (line 341-372)
```python
async def _query_table_results(
    self,
    query: str,
    recognizer_settings: IntentRecognizerSettings,
) -> Dict[str, List[Any]]:
    """并发查询映射中所有表"""
    if self.ragflow_client is None:
        raise ValueError("RagflowClient not initialized")

    if not hasattr(self.ragflow_client, "query_single_database"):
        raise ValueError("RagflowClient missing query_single_database method")

    table_names = list(recognizer_settings.database_mapping.keys())  # 当前全量查询
    tasks = [
        self.ragflow_client.query_single_database(query, table_name)
        for table_name in table_names
    ]
    task_results = await asyncio.gather(*tasks, return_exceptions=True)

    # ... 错误处理 ...
```

# Database mapping structure (from config.yaml)
```yaml
database_mapping:
  岱山-指令集: 1
  岱山-数据库问题: 2
  岱山-指令集-固定问题: 3
```

# Type ID to database name mapping
- type_id=1 → "岱山-指令集"
- type_id=2 → "岱山-数据库问题"
- type_id=3 → "岱山-指令集-固定问题"
</context>

<feature>
  <name>Classification-Driven Retrieval</name>
  <files>
    src/rag_stream/src/config/settings.py
    src/rag_stream/config.yaml
    src/rag_stream/src/services/intent_service/intent_service.py
    src/rag_stream/tests/test_intent_service_classification.py
  </files>
  <behavior>
    ### Test 1: High Confidence Filtering (>= 0.7)
    Input: query="如何执行岱山指令集", classification.type_id=1, confidence=0.85
    Expected: database_mapping filtered to only {"岱山-指令集": 1}

    ### Test 2: Low Confidence Fallback (< 0.7)
    Input: query="如何执行岱山指令集", classification.type_id=1, confidence=0.5
    Expected: database_mapping remains full (all databases)

    ### Test 3: Degraded Classification Fallback
    Input: query="如何执行岱山指令集", classification.degraded=True
    Expected: database_mapping remains full (all databases)

    ### Test 4: Disabled Classifier Fallback
    Input: query="如何执行岱山指令集", self._intent_classifier=None
    Expected: database_mapping remains full (all databases)

    ### Test 5: Confidence Threshold from Config
    Input: confidence=0.7, threshold=0.7
    Expected: classification applied (>= threshold uses filtering)

    ### Test 6: Type ID Mapping Correctness
    Input: type_id=2 (database problem)
    Expected: database_mapping filtered to {"岱山-数据库问题": 2}
  </behavior>
  <implementation>
    1. Update confidence_threshold in config.yaml to 0.7
    2. Modify _load_process_settings to accept text_input parameter
    3. Add classification logic: if classifier enabled, call classify()
    4. Filter database_mapping based on confidence and type_id
    5. Add marker logging for fallback scenarios
  </implementation>
</feature>

<tasks>

<task type="auto">
  <name>Task 1: 更新置信度阈值配置</name>
  <files>src/rag_stream/config.yaml</files>
  <action>
    更新 config.yaml 中的 confidence_threshold 值从 0.5 改为 0.7。

    注意：IntentClassificationConfig 类和 confidence_threshold 字段已在 Phase 5 的 settings.py 中实现，仅需更新配置文件默认值。
  </action>
  <verify>
    <automated>grep -n "confidence_threshold" src/rag_stream/config.yaml | grep "0.7"</automated>
  </verify>
  <done>config.yaml 中 confidence_threshold 值为 0.7</done>
</task>

<task type="auto">
  <name>Task 2: 编写分类驱动检索的单元测试</name>
  <files>src/rag_stream/tests/test_intent_service_classification.py</files>
  <action>
    创建测试文件 test_intent_service_classification.py，包含以下测试用例：

    1. test_high_confidence_filters_database_mapping:
       - Mock IntentClassifier.classify 返回 type_id=1, confidence=0.85
       - 调用 IntentService._load_process_settings(text_input)
       - 验证返回的 recognizer_settings.database_mapping 仅包含 {"岱山-指令集": 1}

    2. test_low_confidence_returns_full_mapping:
       - Mock IntentClassifier.classify 返回 type_id=1, confidence=0.5
       - 调用 IntentService._load_process_settings(text_input)
       - 验证返回的 recognizer_settings.database_mapping 包含所有数据库

    3. test_degraded_classification_returns_full_mapping:
       - Mock IntentClassifier.classify 返回 degraded=True
       - 调用 IntentService._load_process_settings(text_input)
       - 验证返回的 recognizer_settings.database_mapping 包含所有数据库
       - 验证 marker "classifier.degraded_fallback" 被记录

    4. test_disabled_classifier_returns_full_mapping:
       - 设置 IntentService._intent_classifier = None
       - 调用 IntentService._load_process_settings(text_input)
       - 验证返回的 recognizer_settings.database_mapping 包含所有数据库

    5. test_confidence_threshold_respected:
       - Mock IntentClassifier.classify 返回 type_id=2, confidence=0.7
       - 验证 database_mapping 被过滤为 {"岱山-数据库问题": 2}

    6. test_type_id_mapping_correctness:
       - Mock IntentClassifier.classify 返回 type_id=3, confidence=0.8
       - 验证 database_mapping 被过滤为 {"岱山-指令集-固定问题": 3}

    使用 pytest 和 unittest.mock 进行 mock。
  </action>
  <verify>
    <automated>pytest src/rag_stream/tests/test_intent_service_classification.py -x</automated>
  </verify>
  <done>所有测试用例已编写并失败（RED 状态）</done>
</task>

<task type="auto">
  <name>Task 3: 实现分类驱动检索逻辑</name>
  <files>src/rag_stream/src/services/intent_service/intent_service.py</files>
  <action>
    在 IntentService 中实现分类驱动的检索逻辑：

    1. **修改 _load_process_settings 方法签名**：
       - 添加 text_input: str 参数（从调用方传入，默认 None 保持向后兼容）
       - 保持现有返回值类型 IntentRecognizerSettings

    2. **实现分类调用逻辑**（在 return recognizer_settings 之前）：
       ```python
       # 分类驱动检索（Phase 6）
       if text_input and self._intent_classifier:
           classification_result = self._intent_classifier.classify(text_input)
           threshold = self.settings.intent_classification.confidence_threshold

           # 降级或低置信度时，保持全量检索
           if classification_result.degraded:
               marker("classifier.degraded_fallback", {"query": text_input, "reason": "degraded"})
               return recognizer_settings

           if classification_result.confidence < threshold:
               marker("classifier.low_confidence_fallback", {
                   "query": text_input,
                   "confidence": classification_result.confidence,
                   "threshold": threshold
               })
               return recognizer_settings

           # 高置信度时，过滤 database_mapping
           type_id = classification_result.type_id
           filtered_mapping = {
               k: v for k, v in recognizer_settings.database_mapping.items()
               if v == type_id
           }

           if filtered_mapping:  # 有匹配的库
               # 创建新的 IntentRecognizerSettings 实例（保持不可变性）
               recognizer_settings = dataclasses.replace(
                   recognizer_settings,
                   database_mapping=filtered_mapping
               )
           # else: 没有匹配的库，保持原映射（防御性编程）
       ```

    3. **识别并修改调用方**：
       - 找到 _load_process_settings 的调用点（在 recognize_intent 或 query_process 方法中）
       - 传入 text_input 参数（通常是 user_query 或 query 参数）

    4. **保持向后兼容**：
       - text_input 参数默认值为 None
       - None 或 self._intent_classifier 为 None 时，返回完整的 recognizer_settings
  </action>
  <verify>
    <automated>pytest src/rag_stream/tests/test_intent_service_classification.py -x</automated>
  </verify>
  <done>
    - _load_process_settings 接收 text_input 参数
    - 调用 IntentClassifier.classify 进行分类
    - 置信度 >= 0.7 时过滤 database_mapping，仅保留对应类型的库
    - 置信度 < 0.7 或降级时返回完整 database_mapping
    - 降级和低置信度场景记录 marker 日志
    - 所有测试通过
  </done>
</task>

</tasks>

<verification>
1. 配置验证：confidence_threshold 值为 0.7
2. 测试覆盖验证：pytest 运行通过，覆盖 6 个测试场景
3. 分类调用验证：grep 搜索 classify() 调用点，确认在 _load_process_settings 中
4. 过滤逻辑验证：检查 database_mapping 过滤逻辑，确认使用字典推导式和 dataclasses.replace
5. Marker 验证：搜索 "classifier.degraded_fallback" 和 "classifier.low_confidence_fallback" 标记
6. 向后兼容验证：未启用分类时，database_mapping 保持完整
</verification>

<success_criteria>
1. 配置文件 confidence_threshold 值为 0.7
2. 单元测试覆盖分类成功路径（高置信度过滤）
3. 单元测试覆盖分类失败降级路径（低置信度、降级、未启用）
4. IntentService 在检索前调用分类器获取 type_id 和 confidence
5. 置信度 >= 0.7 时，仅检索对应类型的向量库（通过过滤 database_mapping 实现）
6. 置信度 < 0.7 或降级时，执行全量检索（向后兼容）
7. 降级和低置信度场景记录 marker 日志，便于监控
8. 分类失败不影响主流程，系统保持可用性
</success_criteria>

<output>
After completion, create `.planning/phases/06-分类驱动检索/06-01-SUMMARY.md`
</output>
