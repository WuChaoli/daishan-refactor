---
phase: 03-测试回归
plan: 01
type: execute
wave: 1
depends_on:
  - 02-01
files_modified:
  - src/rag_stream/tests/test_chat_general_service.py
autonomous: true
requirements:
  - TEST-01
  - TEST-02
must_haves:
  truths:
    - AI 改写成功路径有单测覆盖（mock AI 返回有效改写结果）
    - AI 异常回退路径有单测覆盖（mock AI 抛出异常、返回空值）
    - 测试在 .venv/uv 环境可执行通过
    - 测试不依赖真实 AI 服务（使用 mock）
  artifacts:
    - path: src/rag_stream/tests/test_chat_general_service.py
      provides: 单测覆盖
      contains: test_replace_economic_zone_success, test_replace_economic_zone_fallback
  key_links:
    - from: test_replace_economic_zone_success
      to: replace_economic_zone
      via: 异步函数调用
    - from: test_replace_economic_zone_fallback
      to: asyncio.to_thread 异常捕获
      via: 异常场景 mock
---

<objective>
为 `replace_economic_zone` 添加单元测试，覆盖 AI 改写成功路径和异常回退路径，确保测试可在 .venv/uv 环境执行。

Purpose: 建立最小测试证据，确保 Phase 2 的改造可回归。  
Output: 增强的 test_chat_general_service.py，包含 success 和 fallback 路径的测试。
</objective>

<execution_context>
@/home/wuchaoli/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-ai-改写接入/02-01-SUMMARY.md

## Key Interfaces to Test

From `src/rag_stream/src/services/chat_general_service.py`:
```python
async def replace_economic_zone(query: str) -> str:
    """使用 AI 对 query 进行企业名称清理。失败时返回原 query。"""
    # 1. 验证输入和配置
    # 2. 通过 asyncio.to_thread 调用 rewrite_query_remove_company
    # 3. 异常时返回原 query（降级）
```

From `src/rag_stream/src/utils/query_chat.py`:
```python
def rewrite_query_remove_company(query: str) -> str:
    """同步函数，实际调用 AI API。"""
```

## Current Test Pattern

Existing tests in `test_chat_general_service.py` use:
- `unittest.mock.patch` for mocking
- `AsyncMock` for async functions
- `pytest` as test runner
- Path setup via `conftest.py`

## Requirements

- **TEST-01**: 单测覆盖 AI 改写成功路径
- **TEST-02**: 单测覆盖 AI 异常时原句回退路径
</context>

<tasks>

<task type="auto">
  <name>Task 1: 添加 AI 改写成功路径测试</name>
  <files>src/rag_stream/tests/test_chat_general_service.py</files>
  <action>
    1. 在 `test_chat_general_service.py` 中添加成功路径测试：
       ```python
       async def _test_replace_economic_zone_success():
           """TEST-01: AI 改写成功路径测试"""
           # Mock settings 配置有效
           with patch("src.services.chat_general_service.settings") as mock_settings:
               mock_settings.query_chat.enabled = True
               mock_settings.query_chat.api_key = "test-key"
               mock_settings.query_chat.base_url = "https://api.example.com"
               
               # Mock AI 返回成功改写结果
               with patch("src.services.chat_general_service.rewrite_query_remove_company") as mock_rewrite:
                   mock_rewrite.return_value = "查询安全情况"
                   
                   # 输入包含企业名称
                   result = await replace_economic_zone("某某公司安全情况查询")
                   
                   # 验证返回改写后的结果
                   assert result == "查询安全情况"
                   mock_rewrite.assert_called_once_with("某某公司安全情况查询")
       
       def test_replace_economic_zone_success():
           asyncio.run(_test_replace_economic_zone_success())
       ```
    
    2. 添加必要的导入（如果缺失）：
       ```python
       from unittest.mock import patch
       ```
    
    3. 确保测试覆盖以下场景：
       - 输入包含企业名称（触发 AI 改写）
       - 配置已启用且有效
       - AI 返回有效改写结果
       - 返回改写后的纯文本句子
  </action>
  <verify>
    <automated>cd src/rag_stream && uv run pytest tests/test_chat_general_service.py::test_replace_economic_zone_success -xvs</automated>
  </verify>
  <done>
    - test_replace_economic_zone_success 测试存在且通过
    - 测试覆盖 AI 成功改写场景
    - 使用 mock 不依赖真实 AI 服务
  </done>
</task>

<task type="auto">
  <name>Task 2: 添加 AI 异常回退路径测试</name>
  <files>src/rag_stream/tests/test_chat_general_service.py</files>
  <action>
    1. 添加 API 异常回退测试：
       ```python
       async def _test_replace_economic_zone_api_error_fallback():
           """TEST-02: AI API 异常时回退原句"""
           with patch("src.services.chat_general_service.settings") as mock_settings:
               mock_settings.query_chat.enabled = True
               mock_settings.query_chat.api_key = "test-key"
               mock_settings.query_chat.base_url = "https://api.example.com"
               
               # Mock AI 抛出异常
               with patch("src.services.chat_general_service.rewrite_query_remove_company") as mock_rewrite:
                   mock_rewrite.side_effect = Exception("API timeout")
                   
                   original_query = "某某公司安全情况查询"
                   result = await replace_economic_zone(original_query)
                   
                   # 验证返回原 query（降级）
                   assert result == original_query
       
       def test_replace_economic_zone_api_error_fallback():
           asyncio.run(_test_replace_economic_zone_api_error_fallback())
       ```
    
    2. 添加空值回退测试：
       ```python
       async def _test_replace_economic_zone_empty_response_fallback():
           """TEST-02: AI 返回空值时回退原句"""
           with patch("src.services.chat_general_service.settings") as mock_settings:
               mock_settings.query_chat.enabled = True
               mock_settings.query_chat.api_key = "test-key"
               mock_settings.query_chat.base_url = "https://api.example.com"
               
               # Mock AI 返回空字符串
               with patch("src.services.chat_general_service.rewrite_query_remove_company") as mock_rewrite:
                   mock_rewrite.return_value = ""
                   
                   original_query = "某某公司安全情况查询"
                   result = await replace_economic_zone(original_query)
                   
                   # 验证返回原 query
                   assert result == original_query
       
       def test_replace_economic_zone_empty_response_fallback():
           asyncio.run(_test_replace_economic_zone_empty_response_fallback())
       ```
    
    3. 添加配置无效回退测试（边界情况）：
       ```python
       async def _test_replace_economic_zone_disabled_fallback():
           """TEST-02: 配置禁用时直接返回原句"""
           with patch("src.services.chat_general_service.settings") as mock_settings:
               mock_settings.query_chat.enabled = False
               
               original_query = "某某公司安全情况查询"
               result = await replace_economic_zone(original_query)
               
               # 不调用 AI，直接返回原句
               assert result == original_query
       
       def test_replace_economic_zone_disabled_fallback():
           asyncio.run(_test_replace_economic_zone_disabled_fallback())
       ```
  </action>
  <verify>
    <automated>cd src/rag_stream && uv run pytest tests/test_chat_general_service.py -k "fallback" -xvs</automated>
  </verify>
  <done>
    - test_replace_economic_zone_api_error_fallback 测试存在且通过
    - test_replace_economic_zone_empty_response_fallback 测试存在且通过
    - test_replace_economic_zone_disabled_fallback 测试存在且通过
    - 所有回退路径验证返回原 query
  </done>
</task>

<task type="auto">
  <name>Task 3: 验证所有测试在 uv 环境通过</name>
  <files>src/rag_stream/tests/test_chat_general_service.py</files>
  <action>
    1. 运行所有新增测试：
       ```bash
       cd src/rag_stream
       uv run pytest tests/test_chat_general_service.py::test_replace_economic_zone_success -xvs
       uv run pytest tests/test_chat_general_service.py::test_replace_economic_zone_api_error_fallback -xvs
       uv run pytest tests/test_chat_general_service.py::test_replace_economic_zone_empty_response_fallback -xvs
       uv run pytest tests/test_chat_general_service.py::test_replace_economic_zone_disabled_fallback -xvs
       ```
    
    2. 验证原有测试未被破坏：
       ```bash
       uv run pytest tests/test_chat_general_service.py -x
       ```
    
    3. 确保测试不依赖真实 AI 服务：
       - 验证所有测试使用 mock
       - 确认没有外部 HTTP 调用
    
    4. 代码风格检查：
       - 遵循现有测试文件的命名规范
       - 函数文档字符串说明测试目的
       - 变量命名清晰
  </action>
  <verify>
    <automated>cd src/rag_stream && uv run pytest tests/test_chat_general_service.py -x --tb=short</automated>
  </verify>
  <done>
    - 所有 4 个新增测试通过
    - 原有测试全部通过
    - 测试不使用真实 AI 服务
    - pytest 退出码为 0
  </done>
</task>

</tasks>

<verification>
- test_replace_economic_zone_success 覆盖 TEST-01（AI 改写成功路径）
- 3 个 fallback 测试覆盖 TEST-02（AI 异常回退路径）
- 所有测试在 uv 环境通过
- 测试使用 mock 不依赖外部服务
- 原有测试未被破坏
</verification>

<success_criteria>
- TEST-01: 单测覆盖 AI 改写成功路径（test_replace_economic_zone_success）
- TEST-02: 单测覆盖 AI 异常时原句回退路径（3 个 fallback 测试）
- 测试在 .venv/uv 环境可执行通过（pytest 退出码 0）
</success_criteria>

<output>
After completion, create `.planning/phases/03-测试回归/03-01-SUMMARY.md`
</output>
