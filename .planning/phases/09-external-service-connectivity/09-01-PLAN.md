---
phase: 09-external-service-connectivity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/integration/test_external_service_connectivity.py
  - tests/integration/conftest.py
autonomous: true
requirements:
  - INT-01
  - INT-02
  - INT-03
  - INT-04
must_haves:
  truths:
    - AI API 连通性测试可通过实际调用验证
    - 向量库连通性测试可通过实际检索验证
    - 数据库连通性测试可通过实际查询验证
    - 测试失败时提供清晰的诊断信息
  artifacts:
    - path: tests/integration/test_external_service_connectivity.py
      provides: 外部服务连通性测试套件
      min_lines: 200
    - path: tests/integration/conftest.py
      provides: 共享测试 fixtures 和配置
      min_lines: 50
  key_links:
    - from: test_external_service_connectivity.py
      to: src/rag_stream/src/config/settings.py
      via: load_settings()
      pattern: "load_settings|Settings"
    - from: test_external_service_connectivity.py
      to: src/rag_stream/src/utils/query_chat.py
      via: QueryChat 类
      pattern: "QueryChat|rewrite_query"
    - from: test_external_service_connectivity.py
      to: src/rag_stream/src/utils/ragflow_client.py
      via: RagflowClient 类
      pattern: "RagflowClient|test_connection"
---

<objective>
创建外部服务连通性测试套件，验证 AI API、向量库、数据库的连通性，确保在 CI/CD 环境中可自动运行。

Purpose: 在部署前验证所有外部依赖服务可正常访问，提前发现配置或网络问题。
Output: tests/integration/test_external_service_connectivity.py 测试文件
</objective>

<execution_context>
@/home/wuchaoli/.claude/get-shit-done/workflows/execute-plan.md
@/home/wuchaoli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-external-service-connectivity/09-CONTEXT.md

<interfaces>
From src/rag_stream/src/config/settings.py:
```python
class QueryChatConfig(BaseConfig):
    enabled: bool
    api_key: str
    base_url: str
    model: str
    timeout: int
    temperature: float

class RAGFlowConfig(BaseConfig):
    api_key: str
    base_url: str
    timeout: int
    max_retries: int
    database_mapping: Dict[str, Union[int, str]]

class Settings(BaseModel):
    ragflow: RAGFlowConfig
    query_chat: QueryChatConfig
    mysql: MySQLConfig
    intent_classification: IntentClassificationConfig
```

From src/rag_stream/src/utils/query_chat.py:
```python
class QueryChat:
    def __init__(self, config: QueryChatConfig):
        self._config = config
        self._client: Optional[OpenAI] = None

    def rewrite_query_remove_company(self, query: str) -> str:
        # 调用 AI API 改写 query
        pass

async def rewrite_query(query: str, config: QueryChatConfig) -> str:
    # 异步包装
    pass
```

From src/rag_stream/src/utils/ragflow_client.py:
```python
class RagflowClient:
    def __init__(self, ragflow_config: RAGFlowConfig, intent_config: IntentConfig):
        self.client = RAGFlowClient(...)

    def test_connection(self) -> bool:
        # 测试 RAGFlow 连接
        pass

    async def query_single_database(self, query: str, database: str) -> List[RetrievalResult]:
        # 查询单个知识库
        pass
```

From src/DaiShanSQL/DaiShanSQL/SQL/sql_utils.py:
```python
class MySQLManager:
    def __init__(self, config_file: str = ".env"):
        self.host = os.getenv('DB_HOST', 'localhost')
        self.database = os.getenv('DB_NAME')
        self.user = os.getenv('DB_USER')
        self.password = os.getenv('DB_PASSWORD')
        self.port = int(os.getenv('DB_PORT', 3306))
        self.api_url_ds = os.getenv('SQL_DataBase')

    def request_api_sql(self, sql):
        # 通过 API 执行 SQL
        pass
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建共享测试配置和 fixtures</name>
  <files>tests/integration/conftest.py</files>
  <behavior>
    - 测试配置加载: 从 config.yaml 和 .env 加载配置
    - 环境变量跳过控制: SKIP_AI_TEST, SKIP_VECTOR_TEST, SKIP_DB_TEST
    - 诊断信息收集: 收集环境变量状态、配置状态
    - 超时设置: 10 秒超时快速失败
  </behavior>
  <action>
创建 tests/integration/conftest.py，包含:
1. 共享 pytest fixtures:
   - `settings`: 加载后的 Settings 实例
   - `event_loop`: 异步事件循环

2. 诊断信息辅助函数:
   - `get_env_status()`: 返回环境变量状态（是否设置，不暴露值）
   - `get_config_status()`: 返回配置状态（enabled, base_url 等）
   - `format_diagnostic_message(service, error, env_status, config_status)`: 格式化诊断信息

3. 跳过控制:
   - 读取 SKIP_AI_TEST, SKIP_VECTOR_TEST, SKIP_DB_TEST 环境变量
   - 提供 `should_skip_ai()`, `should_skip_vector()`, `should_skip_db()` 辅助函数

4. 路径处理:
   - 自动添加 src/rag_stream/src 到 sys.path
   - 自动添加 src/DaiShanSQL 到 sys.path

注意: 诊断信息中 API 密钥等敏感信息只显示是否设置，不显示实际值。
  </action>
  <verify>
    <automated>cd /home/wuchaoli/codespace/daishan-refactor && python -c "import sys; sys.path.insert(0, 'tests/integration'); import conftest; print('conftest.py loads successfully')"</automated>
  </verify>
  <done>conftest.py 文件存在，包含 settings fixture 和诊断辅助函数，路径配置正确</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 AI API 连通性测试</name>
  <files>tests/integration/test_external_service_connectivity.py</files>
  <behavior>
    - 测试 QueryChat 可成功调用 AI API
    - 测试 Intent Classification API 可成功调用
    - 验证 API 认证有效（不返回 401/403）
    - 超时控制在 10 秒内
    - 失败时输出诊断信息（环境变量状态、配置状态）
  </behavior>
  <action>
在 test_external_service_connectivity.py 中实现 AI API 连通性测试:

1. 导入依赖:
   - from src.rag_stream.src.config.settings import load_settings, QueryChatConfig
   - from src.rag_stream.src.utils.query_chat import QueryChat
   - asyncio 用于异步测试

2. 实现 `TestAIAPIConnectivity` 测试类:

   a. `test_query_chat_connectivity()`:
      - 检查 SKIP_AI_TEST 环境变量，设置则 pytest.skip()
      - 加载 settings，获取 query_chat 配置
      - 验证配置: enabled=True, api_key 已设置, base_url 已设置
      - 创建 QueryChat 实例
      - 调用 rewrite_query_remove_company("测试企业岱山经开区") 发送测试请求
      - 验证: 不抛出异常，返回非空字符串
      - 超时: 使用 asyncio.wait_for 设置 10 秒超时
      - 失败时: 调用 format_diagnostic_message 输出诊断信息

   b. `test_intent_classification_connectivity()`:
      - 检查 SKIP_AI_TEST 环境变量
      - 验证 intent_classification 配置
      - 使用 IntentClassifier 发送简单分类请求
      - 验证: 返回分类结果（不关心具体类型，只验证连通性）
      - 超时: 10 秒

3. 诊断信息输出格式:
   ```
   AI API 连通性测试失败
   ======================
   错误: {error_message}

   环境变量检查:
   - QUERY_CHAT_API_KEY: 已设置 / 未设置
   - QUERY_CHAT_BASE_URL: 已设置 / 未设置

   配置检查:
   - enabled: True / False
   - base_url: {base_url}
   - model: {model}

   建议:
   - 检查 API 密钥是否有效
   - 检查网络连通性: ping {base_url_host}
   ```

注意: 使用 pytest.mark.asyncio 标记异步测试函数。
  </action>
  <verify>
    <automated>cd /home/wuchaoli/codespace/daishan-refactor && python -m pytest tests/integration/test_external_service_connectivity.py::TestAIAPIConnectivity -v --tb=short 2>&1 | head -50</automated>
  </verify>
  <done>AI API 连通性测试实现完成，包含 QueryChat 和 Intent Classification 测试，失败时输出诊断信息</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 实现向量库和数据库连通性测试</name>
  <files>tests/integration/test_external_service_connectivity.py</files>
  <behavior>
    - 测试 RAGFlow 向量库连接正常
    - 测试可执行基础向量检索
    - 测试达梦数据库连接正常
    - 测试可执行基础 SQL 查询
    - 失败时输出诊断信息
  </behavior>
  <action>
在 test_external_service_connectivity.py 中继续实现向量库和数据库连通性测试:

1. 实现 `TestVectorStoreConnectivity` 测试类:

   a. `test_ragflow_connection()`:
      - 检查 SKIP_VECTOR_TEST 环境变量
      - 加载 settings，获取 ragflow 配置
      - 验证配置: api_key 已设置, base_url 已设置
      - 创建 RagflowClient 实例（mock intent_config 或传递真实配置）
      - 调用 test_connection() 方法
      - 验证: 返回 True
      - 失败时输出诊断信息

   b. `test_vector_retrieval()`:
      - 检查 SKIP_VECTOR_TEST 环境变量
      - 获取第一个配置的知识库名称（从 database_mapping）
      - 调用 query_single_database("测试查询", database_name)
      - 验证: 不抛出异常，返回列表（可为空）
      - 超时: 10 秒

2. 实现 `TestDatabaseConnectivity` 测试类:

   a. `test_database_connection()`:
      - 检查 SKIP_DB_TEST 环境变量
      - 从环境变量获取 DB_HOST, DB_NAME, DB_USER, DB_PASSWORD, DB_PORT
      - 验证: 必需的环境变量已设置
      - 创建 MySQLManager 实例
      - 执行简单查询: request_api_sql("SELECT 1 as test")
      - 验证: 返回结果不为空
      - 失败时输出诊断信息

3. 诊断信息格式:
   - 向量库诊断: 显示 RAGFLOW_API_KEY 是否设置、RAGFLOW_BASE_URL、配置的知识库列表
   - 数据库诊断: 显示 DB_HOST, DB_NAME, DB_PORT 是否设置（不显示密码）

4. 测试文件结构:
   ```python
   # 文件顶部: 路径设置和导入
   # conftest.py 提供共享 fixtures

   class TestAIAPIConnectivity:
       # AI API 测试

   class TestVectorStoreConnectivity:
       # 向量库测试

   class TestDatabaseConnectivity:
       # 数据库测试

   # 辅助函数: 诊断信息格式化
   ```

注意: 所有测试函数都要处理异常，确保失败时输出有用的诊断信息而不是堆栈跟踪。
  </action>
  <verify>
    <automated>cd /home/wuchaoli/codespace/daishan-refactor && python -m pytest tests/integration/test_external_service_connectivity.py -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <done>向量库和数据库连通性测试实现完成，所有 4 个需求 INT-01~INT-04 覆盖完整</done>
</task>

</tasks>

<verification>
整体验证步骤:
1. 运行全部连通性测试: `python -m pytest tests/integration/test_external_service_connectivity.py -v`
2. 验证跳过机制: `SKIP_AI_TEST=1 pytest ...` 确认 AI 测试被跳过
3. 验证诊断信息: 临时修改配置为无效值，确认失败时输出清晰诊断
4. 验证超时: 确认测试在 10 秒内完成或失败
</verification>

<success_criteria>
- 测试文件 tests/integration/test_external_service_connectivity.py 存在且可运行
- 包含 AI API、向量库、数据库三类连通性测试
- 测试失败时提供清晰的诊断信息（环境变量状态、配置状态、建议）
- 支持通过环境变量跳过特定测试（SKIP_AI_TEST, SKIP_VECTOR_TEST, SKIP_DB_TEST）
- 超时设置为 10 秒，适合 CI/CD 环境
- 所有需求 INT-01, INT-02, INT-03, INT-04 已覆盖
</success_criteria>

<output>
After completion, create `.planning/phases/09-external-service-connectivity/09-01-SUMMARY.md`
</output>
