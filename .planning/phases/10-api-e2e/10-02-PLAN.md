---
phase: 10-api-e2e
plan: 02
type: execute
wave: 1
depends_on: ["10-01"]
files_modified:
  - tests/e2e/test_api_general_ci.py
  - tests/data/intent_test_cases.xlsx
  - tests/data/
autonomous: true
gap_closure: true
requirements: ["INT-05"]

must_haves:
  truths:
    - "意图分类结果被正确解析和验证"
    - "测试数据文件在预期位置存在"
  artifacts:
    - path: "tests/e2e/test_api_general_ci.py"
      provides: "包含 parse_intent_classification_logs() 和 extract_classification_result() 函数"
      contains: "def parse_intent_classification_logs"
    - path: "tests/data/intent_test_cases.xlsx"
      provides: "测试数据文件"
      exists: true
  key_links:
    - from: "run_single_test()"
      to: "classification_type/classification_confidence"
      via: "调用解析函数并赋值"
---

<objective>
修复 Phase 10 E2E 测试中的两个验证 gaps：
1. 添加意图分类结果解析逻辑（从 Phase 08 迁移）
2. 确保测试数据文件在预期位置存在

Purpose: 完成 INT-05 要求的意图分类链路完整验证
Output: 完整的 E2E 测试套件，能够解析和验证意图分类结果
</objective>

<execution_context>
@/home/wuchaoli/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuchaoli/.config/opencode/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/phases/10-api-e2e/10-VERIFICATION.md
@.planning/phases/10-api-e2e/10-01-SUMMARY.md

## Gap Analysis (from VERIFICATION.md)

**Gap 1: 意图分类解析逻辑缺失**
- `ApiTestResult` 定义了 `classification_type` 和 `classification_confidence` 字段（第 67-68 行）
- 这些字段在 `run_single_test()` 中从未被赋值
- Phase 08 实现有 `parse_intent_classification_logs()` 和 `extract_classification_result()` 函数
- 新实现缺少这些函数，导致分类结果无法验证

**Gap 2: 测试数据文件位置不匹配**
- PLAN.md 预期测试数据在 `tests/data/intent_test_cases.xlsx`
- 实际文件在 `src/rag_stream/tests/data/intent_test_cases.xlsx`
- Docker Compose 配置的 `TEST_DATA_PATH` 指向 `tests/data/` 路径

## Reference Implementation (from Phase 08)

位置: `src/rag_stream/tests/test_api_general_e2e.py` 第 141-201 行

```python
def find_latest_log_file(log_dir: Path) -> Path | None:
    """Find the latest log file in the log directory."""
    if not log_dir.exists():
        return None

    log_files = list(log_dir.glob("*.log"))
    if not log_files:
        return None

    return max(log_files, key=lambda p: p.stat().st_mtime)


def parse_intent_classification_logs(log_dir: Path) -> list[dict[str, Any]]:
    """Parse intent classification logs from log files."""
    latest_log = find_latest_log_file(log_dir)
    if not latest_log:
        return []

    classifications = []
    try:
        with open(latest_log, "r", encoding="utf-8") as f:
            for line in f:
                line = line.strip()
                if not line:
                    continue

                try:
                    entry = json.loads(line)
                    marker_type = entry.get("marker", "")
                    if marker_type in ("classifier.attempt", "classifier.success",
                                       "classifier.degraded_fallback", "classifier.low_confidence_fallback"):
                        classifications.append(entry)
                except json.JSONDecodeError:
                    continue
    except Exception:
        pass

    return classifications


def extract_classification_result(logs: list[dict[str, Any]]) -> tuple[int | None, float | None]:
    """Extract classification type and confidence from logs."""
    for entry in reversed(logs):
        if entry.get("marker") == "classifier.success":
            data = entry.get("data", {})
            return data.get("type_id"), data.get("confidence")
    return None, None
```

## Files to Modify

1. `tests/e2e/test_api_general_ci.py` - 添加解析函数并在 run_single_test 中调用
2. `tests/data/` - 创建目录并复制测试数据
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add intent classification log parsing functions</name>
  <files>tests/e2e/test_api_general_ci.py</files>
  <action>
添加以下三个函数到 test_api_general_ci.py（建议在 API Call Functions 和 Test Report Functions 之间）：

1. `find_latest_log_file(log_dir: Path) -> Path | None` - 查找日志目录中最新的 .log 文件
2. `parse_intent_classification_logs(log_dir: Path) -> list[dict[str, Any]]` - 解析意图分类日志，筛选 marker 为 classifier.attempt/classifier.success/classifier.degraded_fallback/classifier.low_confidence_fallback 的条目
3. `extract_classification_result(logs: list[dict[str, Any]]) -> tuple[int | None, float | None]` - 从日志中提取 type_id 和 confidence

参考 Phase 08 的实现（src/rag_stream/tests/test_api_general_e2e.py 第 141-201 行），但要适配 CI 环境：
- 日志目录通过环境变量 LOG_DIR 或默认 .log-manager/runs/
- 处理文件不存在的情况，返回空列表/None 而不是抛出异常
- 添加适当的类型注解和文档字符串

然后在 `run_single_test()` 函数中，在 `result.success = True` 之后添加：
```python
# Parse intent classification from logs
log_dir = Path(os.environ.get("LOG_DIR", ".log-manager/runs"))
classification_logs = parse_intent_classification_logs(log_dir)
class_type, confidence = extract_classification_result(classification_logs)
result.classification_type = class_type
result.classification_confidence = confidence
```
  </action>
  <verify>
    grep -n "def parse_intent_classification_logs" tests/e2e/test_api_general_ci.py &&
    grep -n "def extract_classification_result" tests/e2e/test_api_general_ci.py &&
    grep -n "result.classification_type = class_type" tests/e2e/test_api_general_ci.py
  </verify>
  <done>
    - parse_intent_classification_logs() 函数存在且有完整实现
    - extract_classification_result() 函数存在且有完整实现
    - run_single_test() 中调用了解析函数并赋值到 result.classification_type 和 result.classification_confidence
  </done>
</task>

<task type="auto">
  <name>Task 2: Create tests/data directory and copy test data</name>
  <files>
    - tests/data/
    - tests/data/intent_test_cases.xlsx
  </files>
  <action>
创建测试数据目录并确保测试数据文件存在：

1. 创建目录: `mkdir -p tests/data/`

2. 检查源文件是否存在: `src/rag_stream/tests/data/intent_test_cases.xlsx`
   - 如果存在：复制到 `tests/data/intent_test_cases.xlsx`
   - 如果不存在：创建最小测试数据集（包含至少 3 个测试用例）

3. 确保文件可以通过默认 TEST_DATA_PATH="tests/data/intent_test_cases.xlsx" 访问

4. 更新 .gitignore（如果需要），确保测试数据文件可以被提交（如果它是测试必需的小文件）或记录为 LFS（如果是大文件）
  </action>
  <verify>
    ls -la tests/data/intent_test_cases.xlsx &&
    python -c "import pandas as pd; df = pd.read_excel('tests/data/intent_test_cases.xlsx'); print(f'Rows: {len(df)}, Columns: {list(df.columns)}')"
  </verify>
  <done>
    - tests/data/ 目录存在
    - tests/data/intent_test_cases.xlsx 文件存在且可读
    - 文件包含必需的列：question, expected_type, description
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify gap closure with dry-run test</name>
  <files>tests/e2e/test_api_general_ci.py</files>
  <action>
验证修复是否成功：

1. 语法检查: `python -m py_compile tests/e2e/test_api_general_ci.py`

2. 类型检查（如有 mypy）: `python -m mypy tests/e2e/test_api_general_ci.py --ignore-missing-imports` 或至少确保 import 正确

3. 验证函数可导入:
   ```python
   python -c "
   import sys
   sys.path.insert(0, 'tests/e2e')
   from test_api_general_ci import parse_intent_classification_logs, extract_classification_result, load_test_cases
   print('All functions import successfully')
   cases = load_test_cases('tests/data/intent_test_cases.xlsx')
   print(f'Loaded {len(cases)} test cases')
   "
   ```

4. 检查 run_single_test 函数是否包含分类解析调用（通过 grep 验证）

5. 更新 10-VERIFICATION.md 中的 gaps 状态（如果该文件需要更新）- 这部分是可选的，因为 verification 是独立的
  </action>
  <verify>
    python -m py_compile tests/e2e/test_api_general_ci.py &&
    python -c "import sys; sys.path.insert(0, 'tests/e2e'); from test_api_general_ci import parse_intent_classification_logs, extract_classification_result; print('Import OK')"
  </verify>
  <done>
    - Python 语法检查通过
    - 新增的函数可以成功导入
    - 测试数据可以正常加载
  </done>
</task>

</tasks>

<verification>
Gap closure verification:
1. VERIFICATION.md Gap 1 修复: classification_type/classification_confidence 字段在测试运行后被正确赋值
2. VERIFICATION.md Gap 2 修复: tests/data/intent_test_cases.xlsx 文件存在且可访问
3. 代码可以通过 Python 语法检查和基本导入测试
</verification>

<success_criteria>
- parse_intent_classification_logs() 函数添加到 test_api_general_ci.py
- extract_classification_result() 函数添加到 test_api_general_ci.py
- run_single_test() 调用解析函数并设置 classification_type/confidence
- tests/data/ 目录存在
- tests/data/intent_test_cases.xlsx 文件存在且包含有效测试数据
- 代码通过语法检查
</success_criteria>

<output>
After completion, create `.planning/phases/10-api-e2e/10-02-SUMMARY.md`
</output>
