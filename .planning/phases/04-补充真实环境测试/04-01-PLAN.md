---
phase: 04-补充真实环境测试
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "src/rag_stream/tests/test_real_env.py",
  "src/rag_stream/tests/data/real_env_test_cases.json",
  "scripts/test_real_env.py"
]
autonomous: true
requirements: []
must_haves:
  truths:
    - "真实环境测试脚本可以手动触发"
    - "测试覆盖典型企业名改写场景"
    - "测试覆盖边界情况"
    - "测试执行时间 < 30 秒"
    - "测试失败时返回非零退出码"
  artifacts:
    - path: "src/rag_stream/tests/test_real_env.py"
      provides: "真实环境测试脚本"
      min_lines: 50
    - path: "src/rag_stream/tests/data/real_env_test_cases.json"
      provides: "测试数据集"
      contains: "test_cases"
    - path: "scripts/test_real_env.py"
      provides: "命令行入口"
      exports: ["main"]
  key_links:
    - from: "scripts/test_real_env.py"
      to: "src/rag_stream/tests/test_real_env.py"
      via: "import and execute"
    - from: "test_real_env.py"
      to: "query_chat.rewrite_query"
      via: "direct function call"
---

<objective>
创建真实环境测试脚本，使用真实 AI 服务验证端到端链路，覆盖典型企业名改写场景和边界情况。

Purpose: 补充 mock 测试的不足，确保 AI 改写功能在真实环境中正常工作。
Output: 可执行的真实环境测试脚本和测试数据集。
</objective>

<execution_context>
@/home/wuchaoli/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/wuchaoli/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-补充真实环境测试/04-CONTEXT.md

@src/rag_stream/src/utils/query_chat.py
@src/rag_stream/src/config/settings.py

## Key Interfaces

From `src/rag_stream/src/utils/query_chat.py`:
```python
async def rewrite_query(query: str, config: QueryChatConfig) -> str:
    """异步改写 query，删除企业名称。
    
    Args:
        query: 原始查询字符串
        config: QueryChat 配置对象
    
    Returns:
        改写后的字符串，失败时返回原 query
    """

class QueryChatConfig(BaseConfig):
    enabled: bool = Field(default=True)
    api_key: str = Field(default="")
    base_url: str = Field(default="")
    model: str = Field(default="")
    timeout: int = Field(default=20)
    temperature: float = Field(default=0.0)
```

From `src/rag_stream/src/config/settings.py`:
```python
def load_settings() -> Settings:
    """加载全局配置"""
    
settings = load_settings()  # 全局配置实例
settings.query_chat  # QueryChatConfig 实例
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建测试数据集</name>
  <files>src/rag_stream/tests/data/real_env_test_cases.json</files>
  <action>
创建测试数据集文件 `src/rag_stream/tests/data/real_env_test_cases.json`，包含以下测试场景：

1. **典型企业名改写** (4个测试用例):
   - "岱山石化有限公司今年的产值是多少" → 期望："岱山石化"被移除
   - "舟山港务集团的吞吐量如何" → 期望："舟山港务集团"被移除
   - "浙江石化股份有限公司有几个厂区" → 期望："浙江石化股份有限公司"被移除
   - "大鱼山电厂的装机容量" → 期望："大鱼山电厂"被移除

2. **复杂句子结构** (2个测试用例):
   - 包含多个企业名的句子
   - 企业名在句子中间位置的句子

3. **无企业名 query** (2个测试用例):
   - "今天的天气怎么样" → 期望：保持原样
   - "园区有多少家企业" → 期望：保持原样

4. **边界情况** (3个测试用例):
   - 空字符串
   - 超长 query (> 200 字符)
   - 纯标点符号

每个测试用例格式：
```json
{
  "id": "case_001",
  "category": "typical_company",
  "input": "岱山石化有限公司今年的产值是多少",
  "expected_patterns": {
    "should_not_contain": ["岱山石化有限公司"],
    "should_contain": ["产值", "多少"]
  },
  "description": "典型企业名改写"
}
```
  </action>
  <verify>
    <automated>cat src/rag_stream/tests/data/real_env_test_cases.json | python -c "import json,sys; data=json.load(sys.stdin); print(f'Test cases: {len(data[\"test_cases\"])}')"</automated>
  </verify>
  <done>
    测试数据集文件存在，包含至少 11 个测试用例，覆盖 4 个类别（typical_company, complex_structure, no_company, boundary）
  </done>
</task>

<task type="auto">
  <name>Task 2: 创建真实环境测试脚本</name>
  <files>src/rag_stream/tests/test_real_env.py</files>
  <action>
创建测试脚本 `src/rag_stream/tests/test_real_env.py`，实现以下功能：

1. **加载配置**: 使用 `src.config.settings.load_settings()` 加载真实配置

2. **加载测试数据**: 从 `real_env_test_cases.json` 加载测试用例

3. **执行测试**: 对每个测试用例：
   - 调用 `rewrite_query(query, settings.query_chat)`
   - 验证返回结果符合预期：
     - 检查 `should_not_contain` 中的词确实不存在
     - 检查 `should_contain` 中的词确实存在（边界情况除外）
   - 记录执行时间和结果

4. **测试结果报告**:
   - 输出通过/失败统计
   - 失败时输出详细的输入、输出和期望
   - 总执行时间统计

5. **退出码**:
   - 所有测试通过：返回 0
   - 有测试失败：返回 1

关键实现要点：
- 使用 `asyncio.run()` 运行异步测试
- 添加超时控制（单个测试 30 秒，总体 60 秒）
- 详细的日志输出，便于定位问题
- 支持通过环境变量跳过测试（如 `SKIP_REAL_ENV_TEST=1`）
  </action>
  <verify>
    <automated>python -c "import sys; sys.path.insert(0, 'src/rag_stream'); from tests.test_real_env import main; print('Import successful')"</automated>
  </verify>
  <done>
    测试脚本可以成功导入，包含 test_all() 主函数，实现了加载配置、执行测试、验证结果、输出报告的功能
  </done>
</task>

<task type="auto">
  <name>Task 3: 创建命令行入口</name>
  <files>scripts/test_real_env.py</files>
  <action>
创建命令行入口 `scripts/test_real_env.py`，提供便捷的执行方式：

1. **导入测试模块**: 从 `src.rag_stream.tests.test_real_env` 导入主函数

2. **命令行参数支持**:
   - `--verbose`/`-v`: 详细输出模式
   - `--category`: 只运行指定类别的测试
   - `--list`: 列出所有测试用例
   - `--dry-run`: 模拟运行（不调用 AI）

3. **执行流程**:
   - 检查环境变量 `QUERY_CHAT_API_KEY` 是否存在
   - 如果不存在，输出警告并建议检查配置
   - 调用测试主函数
   - 根据结果设置退出码

4. **使用示例**:
   ```bash
   # 运行所有测试
   python scripts/test_real_env.py
   
   # 详细输出
   python scripts/test_real_env.py --verbose
   
   # 只运行典型企业名测试
   python scripts/test_real_env.py --category typical_company
   
   # 列出测试用例
   python scripts/test_real_env.py --list
   ```
  </action>
  <verify>
    <automated>python scripts/test_real_env.py --list 2>&1 | head -20</automated>
  </verify>
  <done>
    命令行入口可以执行 `--list` 命令显示测试用例，支持基本参数解析
  </done>
</task>

</tasks>

<verification>
1. **测试数据集验证**: 检查 `real_env_test_cases.json` 包含完整的测试用例
2. **脚本导入验证**: 确保 `test_real_env.py` 可以成功导入
3. **命令行验证**: 确保 `scripts/test_real_env.py --list` 可以正常执行
</verification>

<success_criteria>
1. 测试数据集文件存在，包含 11+ 个测试用例，覆盖所有要求场景
2. 真实环境测试脚本可以导入，包含完整的测试逻辑
3. 命令行入口可以列出测试用例
4. 所有文件遵循项目代码规范
</success_criteria>

<output>
After completion, create `.planning/phases/04-补充真实环境测试/04-01-SUMMARY.md`
</output>
</context>
</content>
</invoke>