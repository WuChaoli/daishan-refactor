---
phase: 05-意图分类基础
plan: 03
type: execute
wave: 1
depends_on: ["05-01", "05-02"]
files_modified:
  - src/rag_stream/src/services/intent_service/intent_service.py
  - src/rag_stream/src/services/intent_service/base_intent_service.py
  - src/rag_stream/src/utils/__init__.py
autonomous: true
requirements:
  - CFG-03
  - CFG-04
user_setup: []

must_haves:
  truths:
    - "配置支持环境变量覆盖分类参数"
    - "分类服务集成到 IntentService 流程中"
    - "配置验证机制完善"
    - "分类标记正确记录到日志系统"
  artifacts:
    - path: "src/rag_stream/src/services/intent_service/intent_service.py"
      provides: "分类服务集成"
      contains: "classify.*intent"
      min_lines: 20
    - path: "src/rag_stream/src/config/settings.py"
      provides: "配置验证方法"
      contains: "validate_intent_classification_config"
  key_links:
    - from: "src/rag_stream/src/services/intent_service/intent_service.py"
      to: "src/rag_stream/src/utils/intent_classifier.py"
      via: "import IntentClassifier"
      pattern: "from.*intent_classifier import"
    - from: "settings.py"
      to: "环境变量覆盖"
      via: "INTENT_CLASSIFICATION_*"
      pattern: "INTENT_CLASSIFICATION_"
---

<objective>
完成配置集成与验证，确保分类服务可正确集成到现有意图识别流程

Purpose: 将分类服务集成到 IntentService，完善配置验证和环境变量覆盖机制
Output: 集成代码、配置验证方法、导出更新
</objective>

<execution_context>
@/home/wuchaoli/.claude/get-shit-done/workflows/execute-plan.md
@/home/wuchaoli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-意图分类基础/05-意图分类基础-CONTEXT.md
@.planning/phases/05-意图分类基础/05-01-PLAN.md
@.planning/phases/05-意图分类基础/05-02-PLAN.md
@src/rag_stream/src/services/intent_service/intent_service.py
@src/rag_stream/src/services/intent_service/base_intent_service.py
@src/rag_stream/src/config/settings.py
@src/rag_stream/src/utils/intent_classifier.py
</context>

<interfaces>
<!-- Key interfaces for integrating classification service -->

From src/rag_stream/src/services/intent_service/intent_service.py:
```python
class IntentService(BaseIntentService):
    async def process_query(self, text_input: str, user_id: str) -> dict
    def _load_process_settings(self) -> IntentRecognizerSettings
```

From src/rag_stream/src/config/settings.py:
```python
class Settings(BaseModel):
    intent_classification: IntentClassificationConfig

def validate_query_chat_config(self) -> None
```

From src/rag_stream/src/utils/intent_classifier.py:
```python
class IntentClassifier:
    def classify(self, query: str) -> ClassificationResult
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 在 IntentService 中集成分类服务</name>
  <files>src/rag_stream/src/services/intent_service/intent_service.py</files>
  <action>
在 IntentService 中添加分类服务集成：

1. 在 __init__ 中初始化分类器：
```python
from src.utils.intent_classifier import IntentClassifier

def __init__(self, ragflow_client):
    super().__init__(ragflow_client=ragflow_client)
    self._use_daishan_priority = False
    self._intent_classifier: Optional[IntentClassifier] = None

    if settings.intent_classification.enabled:
        try:
            self._intent_classifier = IntentClassifier(settings.intent_classification)
            marker("intent_classifier.initialized", {"enabled": True})
        except Exception as e:
            marker("intent_classifier.init_failed", {"error": str(e)}, level="WARNING")
            self._intent_classifier = None
```

2. 在 _load_process_settings 中添加分类调用（可选，为 Phase 6 预留接口）：
```python
def _load_process_settings(self) -> IntentRecognizerSettings:
    recognizer_settings = self._load_intent_recognizer_settings()
    self._use_daishan_priority = self._should_use_daishan_priority(recognizer_settings)

    # 预留：可选调用分类服务（Phase 6 使用）
    # if self._intent_classifier:
    #     classification_result = self._intent_classifier.classify(text_input)
    #     ...

    return recognizer_settings
```

3. 确保分类器初始化失败不阻断主流程（捕获异常，降级为 None）。

Phase 5 仅完成集成准备，实际分类调用将在 Phase 6 实现。
  </action>
  <verify>
grep -n "IntentClassifier" src/rag_stream/src/services/intent_service/intent_service.py && grep -n "_intent_classifier" src/rag_stream/src/services/intent_service/intent_service.py
  </verify>
  <done>分类服务集成完成，初始化逻辑不阻断主流程</done>
</task>

<task type="auto">
  <name>Task 2: 添加配置验证方法</name>
  <files>src/rag_stream/src/config/settings.py</files>
  <action>
在 Settings 类中添加配置验证方法：

1. 添加 validate_intent_classification_config 方法（参考 validate_query_chat_config）：
```python
def validate_intent_classification_config(self) -> None:
    """验证 intent_classification 配置，无效时记录告警并降级运行。"""
    logger = logging.getLogger(__name__)
    config = self.intent_classification

    if not config.enabled:
        logger.info("Intent Classification 功能已禁用（enabled=false）")
        return

    missing: list[str] = []
    if not (config.api_key or "").strip():
        missing.append("api_key")
    if not (config.base_url or "").strip():
        missing.append("base_url")
    if not (config.model or "").strip():
        missing.append("model")

    if missing:
        logger.warning(
            "INTENT_CLASSIFICATION 配置不完整（缺少: %s），分类功能将降级运行",
            ", ".join(missing),
        )

    if logger.isEnabledFor(logging.DEBUG):
        logger.debug(
            "Intent Classification 配置检查: enabled=%s, base_url=%s, model=%s, timeout=%s, temperature=%s, confidence_threshold=%s, api_key=%s",
            config.enabled,
            config.base_url,
            config.model,
            config.timeout,
            config.temperature,
            config.confidence_threshold,
            "***" if config.api_key else "",
        )
```

2. 在 load_settings 函数中调用验证：
```python
loaded_settings.validate_query_chat_config()
loaded_settings.validate_intent_classification_config()  # 新增
return loaded_settings
```

3. 在 Settings.load_from_yaml_with_env_override 方法中添加环境变量映射（Plan 01 已完成，确认存在）。

确保验证逻辑与 QueryChatConfig 保持一致。
  </action>
  <verify>
grep -n "validate_intent_classification_config" src/rag_stream/src/config/settings.py
  </verify>
  <done>配置验证方法添加完成，支持降级告警</done>
</task>

<task type="auto">
  <name>Task 3: 更新导出与文档</name>
  <files>src/rag_stream/src/utils/__init__.py</files>
  <action>
更新模块导出：

1. 在 src/rag_stream/src/utils/__init__.py 中添加导出：
```python
from .intent_classifier import IntentClassifier, ClassificationResult
```

2. 确保导出的类可被其他模块导入使用。

3. 在 config.yaml 中添加注释说明环境变量覆盖方式：
```yaml
# 环境变量示例：
# export INTENT_CLASSIFICATION_ENABLED=true
# export INTENT_CLASSIFICATION_API_KEY="your-api-key"
# export INTENT_CLASSIFICATION_MODEL="Qwen2.5-7B-Instruct"
# export INTENT_CLASSIFICATION_TEMPERATURE=0.0
```

完成导出和文档更新。
  </action>
  <verify>
grep -n "IntentClassifier\|ClassificationResult" src/rag_stream/src/utils/__init__.py
  </verify>
  <done>导出更新完成，分类服务可被外部模块使用</done>
</task>

</tasks>

<verification>
1. 分类服务正确初始化并集成到 IntentService
2. 配置验证方法能够检测缺失的配置项
3. 环境变量能够覆盖配置文件中的值
4. 导出的类能够被正确导入
</verification>

<success_criteria>
1. 配置支持环境变量覆盖分类参数
2. 分类服务集成到 IntentService 流程中
3. 配置验证机制完善，缺失配置时记录告警
4. 分类标记正确记录到日志系统
</success_criteria>

<output>
After completion, create `.planning/phases/05-意图分类基础/05-03-SUMMARY.md`
</output>
