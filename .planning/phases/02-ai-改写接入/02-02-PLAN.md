---
phase: 02-ai-改写接入
plan: 02
type: gap_closure
wave: 1
depends_on:
  - 02-01
gap_closure: true
files_modified:
  - src/rag_stream/src/services/chat_general_service.py
autonomous: true
requirements:
  - NORM-01
  - NORM-02
must_haves:
  truths:
    - replace_economic_zone 调用 rewrite_query 而不是 rewrite_query_remove_company
    - 通过 settings.query_chat 传递配置
    - 保持现有异常处理和降级逻辑不变
  artifacts:
    - path: src/rag_stream/src/services/chat_general_service.py
      provides: 业务逻辑集成
      contains: rewrite_query import 和调用
  key_links:
    - from: replace_economic_zone
      to: rewrite_query
      via: await rewrite_query(query, settings.query_chat)
---

<objective>
修复 Phase 2 的关键缺口：将 `replace_economic_zone` 从旧接口迁移到新异步接口 `rewrite_query`。

Purpose: 完成 AI 改写工具与业务逻辑的集成，使新接口真正投入使用。
</objective>

<execution_context>
@/home/wuchaoli/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/02-ai-改写接入/02-01-VERIFICATION.md

## Gap Summary

From VERIFICATION.md:
- **Gap:** `rewrite_query` 已实现但未被 `replace_economic_zone` 调用
- **Current:** `chat_general_service.py:62` 使用 `asyncio.to_thread(rewrite_query_remove_company, query)`
- **Expected:** 应使用 `await rewrite_query(query, settings.query_chat)`

## Current Code

```python
# src/rag_stream/src/services/chat_general_service.py
from src.utils.query_chat import rewrite_query_remove_company  # Line 10

async def replace_economic_zone(query: str) -> str:
    # ... config checks ...
    rewritten = await asyncio.to_thread(rewrite_query_remove_company, query)  # Line 62
```

## Target Code

```python
from src.utils.query_chat import rewrite_query  # New import

async def replace_economic_zone(query: str) -> str:
    # ... config checks ...
    rewritten = await rewrite_query(query, settings.query_chat)  # Use new interface
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: 更新 import 语句</name>
  <files>src/rag_stream/src/services/chat_general_service.py</files>
  <action>
    1. 修改文件顶部的 import：
       ```python
       # 修改前：
       from src.utils.query_chat import rewrite_query_remove_company
       
       # 修改后：
       from src.utils.query_chat import rewrite_query
       ```
    
    2. 检查是否还有其他地方使用 `rewrite_query_remove_company`：
       ```bash
       grep -n "rewrite_query_remove_company" src/rag_stream/src/services/chat_general_service.py
       ```
       
    3. 如果只有一处使用（即要修改的地方），直接替换即可。
       如果有其他引用，需要评估是否也需要修改。
  </action>
  <verify>
    <automated>cd src/rag_stream && python -c "
from src.services.chat_general_service import replace_economic_zone
import inspect
source = inspect.getsource(replace_economic_zone)
assert 'rewrite_query' in source, 'rewrite_query not found in function'
assert 'rewrite_query_remove_company' not in source, 'Old import still present'
print('✓ Import updated successfully')
"</automated>
  </verify>
  <done>
    - import 从 rewrite_query_remove_company 改为 rewrite_query
    - 无其他地方引用旧函数
  </done>
</task>

<task type="auto">
  <name>Task 2: 替换调用语句</name>
  <files>src/rag_stream/src/services/chat_general_service.py</files>
  <action>
    1. 定位 `replace_economic_zone` 函数中的调用：
       ```python
       # 修改前（约第 62 行）：
       rewritten = await asyncio.to_thread(rewrite_query_remove_company, query)
       ```
    
    2. 替换为新调用方式：
       ```python
       # 修改后：
       rewritten = await rewrite_query(query, settings.query_chat)
       ```
    
    3. 如果 `asyncio` 不再被其他地方使用，可以移除 `asyncio` 导入。
       检查：
       ```bash
       grep -n "asyncio" src/rag_stream/src/services/chat_general_service.py
       ```
    
    4. 确保 `settings` 已被导入（应该在文件顶部已有）。
  </action>
  <verify>
    <automated>cd src/rag_stream && python -c "
import asyncio
from src.config.settings import settings
from src.utils.query_chat import rewrite_query

# Test the function signature
async def test_call():
    # This should work without error
    result = await rewrite_query('测试公司', settings.query_chat)
    print(f'✓ Async call works, returned: {type(result).__name__}')

asyncio.run(test_call())
"</automated>
  </verify>
  <done>
    - 调用从 asyncio.to_thread(rewrite_query_remove_company, query) 
      改为 await rewrite_query(query, settings.query_chat)
    - 保持原有的异常处理和降级逻辑
  </done>
</task>

<task type="auto">
  <name>Task 3: 验证完整集成流程</name>
  <files>src/rag_stream/src/services/chat_general_service.py</files>
  <action>
    1. 运行集成测试验证新接口被正确调用：
       ```python
       import asyncio
       from src.services.chat_general_service import replace_economic_zone
       
       async def test_integration():
           # 测试正常 query（不含企业名，应该直接返回）
           result = await replace_economic_zone("你好")
           assert result == "你好"
           print("✓ Non-company query passed through")
           
           # 测试含企业名的 query（应该触发 AI 流程）
           # 由于配置可能无效，结果可能是原句或改写句
           result = await replace_economic_zone("测试公司查询")
           assert isinstance(result, str)
           print("✓ Company query processed")
       
       asyncio.run(test_integration())
       ```
    
    2. 检查日志输出，确认使用了新的 marker 标签：
       - 应该看到 `query_chat.attempt` 等统计标记
    
    3. 验证向后兼容：
       - 确认 `rewrite_query_remove_company` 仍然可用（在其他地方可能还有用）
       
    4. 提交更改：
       ```bash
       git add src/rag_stream/src/services/chat_general_service.py
       git commit -m "fix(02-02): integrate rewrite_query into replace_economic_zone"
       ```
    
    5. 更新 VERIFICATION.md 中的缺口状态：
       - 将 "rewrite_query 被 replace_economic_zone 调用" 标记为 resolved
  </action>
  <verify>
    <automated>cd src/rag_stream && python -c "
import asyncio
from src.services.chat_general_service import replace_economic_zone

async def test():
    # Test 1: Non-company query (should pass through)
    result = await replace_economic_zone('hello')
    assert result == 'hello', f'Expected \"hello\", got \"{result}\"'
    print('✓ Test 1 passed: Non-company query')
    
    # Test 2: Company query (should process)
    result = await replace_economic_zone('测试公司查询')
    assert isinstance(result, str), f'Expected str, got {type(result)}'
    print('✓ Test 2 passed: Company query processed')
    
    print('\n✓ All integration tests passed!')

asyncio.run(test())
"</automated>
  </verify>
  <done>
    - replace_economic_zone 成功调用 rewrite_query
    - 集成测试通过
    - VERIFICATION.md 缺口已标记为 resolved
  </done>
</task>

</tasks>

<verification>
- import 语句已更新为 rewrite_query
- replace_economic_zone 调用 rewrite_query 而非旧接口
- 集成测试通过，完整流程可用
- VERIFICATION.md 缺口状态已更新
</verification>

<success_criteria>
- NORM-01: replace_economic_zone 调用新接口（rewrite_query）✓
- NORM-02: 删除企业名功能通过新接口实现 ✓
- 与 Phase 2 原有功能保持一致 ✓
</success_criteria>

<output>
After completion:
1. Update .planning/phases/02-ai-改写接入/02-01-VERIFICATION.md to mark gaps as resolved
2. Create .planning/phases/02-ai-改写接入/02-02-SUMMARY.md
3. Run /gsd-verify-work 2 to confirm phase completion
</output>
