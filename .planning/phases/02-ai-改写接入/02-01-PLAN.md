---
phase: 02-ai-改写接入
plan: 01
type: execute
wave: 1
depends_on:
  - 01-01
files_modified:
  - src/rag_stream/src/utils/query_chat.py
autonomous: true
requirements:
  - NORM-01
  - NORM-02
  - NORM-03
  - SAFE-01
must_haves:
  truths:
    - query_chat.py 提供异步 rewrite_query 函数
    - 内容验证检测异常响应并回退
    - 统计指标记录成功/失败/异常次数
    - 向后兼容保留原有同步函数
  artifacts:
    - path: src/rag_stream/src/utils/query_chat.py
      provides: 异步改写工具
      contains: rewrite_query, _validate_content, marker 统计
  key_links:
    - from: rewrite_query
      to: replace_economic_zone
      via: 异步调用
---

<objective>
实现异步聊天工具接口，添加内容验证和统计指标，完成 AI 改写接入。

Purpose: 提供符合 CONTEXT.md 规范的异步接口，增强错误处理和可观测性。  
Output: 增强的 query_chat.py，支持异步调用和内容验证。
</objective>

<execution_context>
@/home/wuchaoli/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/02-ai-改写接入/02-CONTEXT.md
@.planning/phases/02-ai-改写接入/02-RESEARCH.md

## Key Interfaces

From query_chat.py:
```python
class QueryChat:
    def rewrite_query_remove_company(self, query: str) -> str:
        # 现有同步实现

def rewrite_query_remove_company(query: str) -> str:
    return get_query_chat().rewrite_query_remove_company(query)
```

Current usage in chat_general_service.py:
```python
rewritten = await asyncio.to_thread(rewrite_query_remove_company, query)
```

Requirements from CONTEXT.md:
- async def rewrite_query(query: str, config: QueryChatConfig) -> str
- 内容异常时回退原句
- 记录统计指标
</context>

<tasks>

<task type="auto">
  <name>Task 1: 添加异步 rewrite_query 函数</name>
  <files>src/rag_stream/src/utils/query_chat.py</files>
  <action>
    1. 在 query_chat.py 中添加异步接口函数：
       ```python
       async def rewrite_query(query: str, config: QueryChatConfig) -> str:
           """异步改写 query，删除企业名称。
           
           Args:
               query: 原始查询字符串
               config: QueryChat 配置对象
               
           Returns:
               改写后的字符串，失败时返回原 query
           """
           # 输入验证
           if not isinstance(query, str):
               marker("rewrite_query.invalid_input", {"input_type": type(query).__name__})
               return query
           
           source_query = query.strip()
           if not source_query:
               return query
           
           # 使用 asyncio.to_thread 包装同步调用
           try:
               chat = QueryChat(config)
               result = await asyncio.to_thread(chat.rewrite_query_remove_company, source_query)
               return result
           except Exception:
               # 任何异常都返回原 query（降级）
               return query
       ```
    
    2. 添加必要的导入：
       ```python
       import asyncio
       ```
    
    3. 确保函数签名和返回类型与 CONTEXT.md 一致。
  </action>
  <verify>
    <automated>cd src/rag_stream && python -c "
import asyncio
from src.config.settings import settings
from src.utils.query_chat import rewrite_query

async def test():
    # 测试无效输入
    result = await rewrite_query(None, settings.query_chat)
    assert result is None, 'Should return invalid input as-is'
    
    # 测试空字符串
    result = await rewrite_query('', settings.query_chat)
    assert result == '', 'Should return empty string as-is'
    
    print('✓ rewrite_query interface test passed')

asyncio.run(test())
"</automated>
  </verify>
  <done>
    - rewrite_query 函数存在且为 async
    - 参数为 (query: str, config: QueryChatConfig)
    - 返回 str
    - 无效输入时返回原值
  </done>
</task>

<task type="auto">
  <name>Task 2: 添加内容验证逻辑</name>
  <files>src/rag_stream/src/utils/query_chat.py</files>
  <action>
    1. 在 QueryChat 类中添加内容验证方法：
       ```python
       @staticmethod
       def _validate_content(text: str, original: str) -> tuple[bool, str]:
           """验证 AI 返回的内容是否有效。
           
           Returns:
               (is_valid, reason) 元组
           """
           if not text:
               return False, "empty"
           
           # 检测 JSON 格式（AI 错误返回）
           if text.strip().startswith(("{", "[")):
               return False, "json"
           
           # 检测指令性文字（AI 在解释而非改写）
           instruction_keywords = ["请", "应该", "建议", "需要", "可以"]
           if any(kw in text for kw in instruction_keywords):
               # 但排除原句本身就包含这些词的情况
               if not any(kw in original for kw in instruction_keywords):
                   return False, "instruction"
           
           # 检测多行文本（可能包含解释）
           if "\n" in text and len(text.split("\n")) > 2:
               return False, "multiline"
           
           return True, ""
       ```
    
    2. 修改 `rewrite_query_remove_company` 方法，在返回前添加验证：
       ```python
       # 在 return rewritten 之前添加：
       is_valid, reason = self._validate_content(rewritten, query)
       if not is_valid:
           marker(
               "query_chat.content_invalid",
               {"reason": reason, "response_preview": rewritten[:100]},
               level="WARNING",
           )
           return query  # 回退原句
       ```
  </action>
  <verify>
    <automated>cd src/rag_stream && python -c "
from src.utils.query_chat import QueryChat
from src.config.settings import QueryChatConfig

config = QueryChatConfig()
chat = QueryChat(config)

# 测试空内容
is_valid, reason = chat._validate_content('', '测试公司')
assert not is_valid and reason == 'empty', f'Expected empty, got {reason}'

# 测试 JSON
is_valid, reason = chat._validate_content('{\"key\": \"value\"}', '测试公司')
assert not is_valid and reason == 'json', f'Expected json, got {reason}'

# 测试有效内容
is_valid, reason = chat._validate_content('测试查询', '测试公司查询')
assert is_valid, f'Expected valid, got {reason}'

print('✓ Content validation test passed')
"</automated>
  </verify>
  <done>
    - _validate_content 方法存在
    - 检测空内容、JSON、指令性文字、多行文本
    - 验证失败时返回原 query
    - 记录 WARNING 日志
  </done>
</task>

<task type="auto">
  <name>Task 3: 增强统计指标</name>
  <files>src/rag_stream/src/utils/query_chat.py</files>
  <action>
    1. 在 `rewrite_query_remove_company` 方法中添加详细统计：
       - 在方法开头记录 `query_chat.attempt`（尝试次数）
       - 在成功返回时记录 `query_chat.success`（成功次数）
       - 在 API 错误时记录 `query_chat.api_error`（API 错误次数）
       - 在内容验证失败时记录 `query_chat.content_invalid`（内容无效次数）
    
    2. 修改后的关键位置 marker 记录：
       ```python
       def rewrite_query_remove_company(self, query: str) -> str:
           # ... 输入验证 ...
           
           marker("query_chat.attempt", {"query_len": len(source_query)})
           
           try:
               # ... API 调用 ...
           except Exception as error:
               marker("query_chat.api_error", {"error": str(error)})
               raise  # 让上层处理回退
           
           # ... 内容验证 ...
           if not is_valid:
               marker("query_chat.content_invalid", {"reason": reason})
               return query
           
           marker("query_chat.success", {"output_len": len(rewritten)})
           return rewritten
       ```
    
    3. 确保所有 marker 标签遵循项目命名规范（小写、下划线分隔）。
  </action>
  <verify>
    <automated>cd src/rag_stream && python -c "
# 检查 marker 调用是否存在
import ast
with open('src/utils/query_chat.py', 'r') as f:
    code = f.read()

tree = ast.parse(code)
marker_calls = []
for node in ast.walk(tree):
    if isinstance(node, ast.Call):
        if isinstance(node.func, ast.Name) and node.func.id == 'marker':
            if node.args:
                marker_calls.append(node.args[0].value if isinstance(node.args[0], ast.Constant) else 'dynamic')

expected_markers = ['attempt', 'success', 'api_error', 'content_invalid']
for m in expected_markers:
    found = any(m in call for call in marker_calls)
    print(f'{\"✓\" if found else \"✗\"} marker for {m}: {\"found\" if found else \"MISSING\"}')

assert all(any(m in call for call in marker_calls) for m in expected_markers), 'Missing markers'
print('✓ All expected markers present')
"</automated>
  </verify>
  <done>
    - query_chat.attempt marker 存在
    - query_chat.success marker 存在
    - query_chat.api_error marker 存在
    - query_chat.content_invalid marker 存在
  </done>
</task>

<task type="auto">
  <name>Task 4: 验证完整流程和向后兼容</name>
  <files>src/rag_stream/src/utils/query_chat.py</files>
  <action>
    1. 确保向后兼容：
       - `rewrite_query_remove_company(query: str)` 全局函数仍然存在
       - `QueryChat.rewrite_query_remove_company` 方法仍然存在
       - 现有调用代码无需修改即可工作
    
    2. 运行集成测试：
       ```python
       # 测试异步接口
       async def test_async_interface():
           result = await rewrite_query("测试公司查询", settings.query_chat)
           # 由于配置可能无效，结果可能是原句或改写句
           assert isinstance(result, str)
       
       # 测试同步接口（向后兼容）
       def test_sync_interface():
           result = rewrite_query_remove_company("测试公司查询")
           assert isinstance(result, str)
       ```
    
    3. 验证代码风格：
       - 遵循 PEP 8
       - 类型注解完整
       - 文档字符串清晰
    
    4. 提交代码：
       ```bash
       git add src/rag_stream/src/utils/query_chat.py
       git commit -m "feat(02): add async rewrite_query with validation and metrics"
       ```
  </action>
  <verify>
    <automated>cd src/rag_stream && python -c "
# 验证向后兼容
from src.utils.query_chat import rewrite_query_remove_company, get_query_chat

# 全局函数存在
assert callable(rewrite_query_remove_company), 'Global function missing'

# QueryChat 类和方法存在
chat = get_query_chat()
assert hasattr(chat, 'rewrite_query_remove_company'), 'Method missing'

# 异步接口存在
import asyncio
from src.utils.query_chat import rewrite_query
assert asyncio.iscoroutinefunction(rewrite_query), 'Not a coroutine'

print('✓ Backward compatibility verified')
print('✓ Async interface verified')
"</automated>
  </verify>
  <done>
    - 同步接口仍然可用
    - 异步接口可用
    - 所有测试通过
  </done>
</task>

</tasks>

<verification>
- rewrite_query 异步函数正确实现
- 内容验证检测空内容、JSON、指令性文字、多行文本
- 统计指标完整（attempt/success/api_error/content_invalid）
- 向后兼容，原有代码无需修改
- 所有降级路径返回原 query
</verification>

<success_criteria>
- NORM-01: 聊天工具存在且可被调用（rewrite_query 异步函数）
- NORM-02: replace_economic_zone 可调用该工具（通过 asyncio.to_thread 或直接 await）
- NORM-03: AI 异常时返回原 query（异常捕获 + 降级逻辑）
- SAFE-01: 改写输出为纯文本句子（内容验证确保）
</success_criteria>

<output>
After completion, create `.planning/phases/02-ai-改写接入/02-01-SUMMARY.md`
</output>
